<h1><%= @task.title %></h1>

<p><%= @task.details  %></p>

<p> <%= @task.completed %> <%= @task.completed ? "This task is completed" : "This task is not completed yet"  %></p>

<div>
  <%= link_to edit_task_path do %>
    <button> ✍︎ Edit task </button>
  <% end %>
</div>

<div>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
</div>

<%= link_to tasks_path do %>
  <small> ⃪ Back to list </small>
<% end %>
