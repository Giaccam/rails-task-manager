<h1> Task list </h1>

<ul>
  <% @tasks.each do |task| %>
      <li> <%= task.completed %> <%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>

<%= link_to new_path do %>
  <small> ✚ Add a new task </small>
<% end %>
